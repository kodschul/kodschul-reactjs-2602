// filter-function

let def = [1, 2, 3, 4, 5, 6];

let filtered = def.filter((num) => num != 3);

console.log(filtered);

process.exit(0);
// map-Function

let abc = [2, 4, 6];
let list2 = abc.map((x) => `<div>${x}</div>`);
console.log(list2);
